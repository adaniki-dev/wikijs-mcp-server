services:
  wikijs-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    image: wikijs-mcp:latest
    container_name: wikijs-mcp
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3200:3200"
